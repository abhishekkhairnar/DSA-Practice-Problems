import java.util.ArrayList;

public class Subset{
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>();
        list.add(1);
        list.add(2);
        list.add(3);
        // call the subset function which will generate all subsequences
        
        int start=0;
        ArrayList<ArrayList<Integer>> ans = new ArrayList<>(); // list to store subsets
        int size = list.size(); // size of given list
        ArrayList<Integer> cur = new ArrayList<>(); // list to store current list


    }

    private static void subSet(ArrayList<Integer> list,ArrayList<ArrayList<Integer>> ans,ArrayList<Integer> cur,int start,int end){
        if(start == end){
            
        }
    }

}